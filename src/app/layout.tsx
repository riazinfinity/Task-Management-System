import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TaskFlow — Task Management System",
  description: "A simple and intuitive task management system to help teams organize daily work, track progress, and stay productive.",
  keywords: ["TaskFlow", "Task Management", "Next.js", "TypeScript", "Tailwind CSS"],
  authors: [{ name: "Qtec Solution Limited" }],
  openGraph: {
    title: "TaskFlow — Task Management System",
    description: "A clean task management system to organize, track, and manage tasks efficiently.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
        suppressHydrationWarning
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
